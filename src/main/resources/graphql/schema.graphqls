type Category {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
  quantitySold: Int!
  createdAt: String
  category: Category
}

type Query {
  categories: [Category]
  productsByCategory(categoryId: ID!): [Product]
}
